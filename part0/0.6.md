sequenceDiagram
    participant browser
    participant server

    Note right of browser: When the save button is pressed, it triggers the javascript code on the browser side to add the note to the existing notes variables and rerender the list, before sending the new data to the server with POST request

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: Server send response : "note created"
    deactivate server

    Note right of browser: Response "note created" is logged to the console from the javascript code